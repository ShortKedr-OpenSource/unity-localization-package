namespace Krugames.LocalizationSystem.RapidStorage.Servers {
    public class TermNote {
        public string TermName;
        public string Note;

        public TermNote() {
        }

        public TermNote(string termName, string note) {
            TermName = termName;
            Note = note;
        }
    }
}